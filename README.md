# Ansh Chhetri — Portfolio

## Run locally
```
npm install
npm run dev
```

## Build for production
```
npm run build
```
Output goes to the `dist` folder.

## Deploy
Push this folder to a GitHub repo, then import it on Vercel or Netlify —
both auto-detect Vite and need no extra config.
